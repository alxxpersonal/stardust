# Vault index

82 notes. Generated by `stardust index`. Read this first, then `stardust query` or open files directly.

- **Go Conventions** - `.claude/rules/go.md`
  - Standard goimports grouping: stdlib, external, local (blank line separators) - Section separators: `// --- Section Name ---` with proper c...
- **Changelog** - `CHANGELOG.md`
  All notable changes to this project will be documented in this file.
- **Stardust** - `CLAUDE.md`
  Local-first, git-backed, markdown-truth personal-context engine for AI agents. Full architecture: [SPEC.md](./SPEC.md).
- **Stardust** - `README.md`
  [![CI](https://github.com/alxxpersonal/stardust/actions/workflows/ci.yml/badge.svg)](https://github.com/alxxpersonal/stardust/actions/workfl...
- **Stardust - Vision & Architecture** - `SPEC.md`
  Status: draft / blueprint. Date: 2026-06-01. A v2 architecture, learning from an earlier overengineered graph-DB attempt. Named after the Te...
- **JSON-RPC 2.0 is the canonical contract transport** - `docs/adr/0001-jsonrpc-canonical-transport.md`
  The stardust contract is JSON-RPC 2.0, not REST and not gRPC.
- **The typed contract package lives in stardust** - `docs/adr/0002-typed-contract-package-in-stardust.md`
  A shared Go package `stardust/rpc` defines the typed method params and results, and exo-jobs imports it.
- **One method registry served over multiple transports** - `docs/adr/0003-one-method-registry-multi-transport.md`
  A single registry of typed JSON-RPC methods is dispatched by both the stdio (MCP) and HTTP transports.
- **Additive-then-retire migration** - `docs/adr/0004-additive-then-retire-migration.md`
  The JSON-RPC registry is added alongside the existing REST, exo-jobs is migrated, then REST is retired. No flag day.
- **Stardust manages docs in both repos** - `docs/adr/0005-stardust-manages-docs-in-both-repos.md`
  Both the stardust repo and the exo-jobs repo use `.stardust` with the docs convention to manage their docs.
- **Use creachadair/jrpc2 and the shared JSON-RPC conventions** - `docs/adr/0006-use-jrpc2-and-shared-conventions.md`
  stardust builds its JSON-RPC layer on `github.com/creachadair/jrpc2`, the same library and conventions the rest of the stack (exo-discord) u...
- **Stardust composes hooks, never clobbers** - `docs/adr/0007-stardust-composes-hooks-never-clobbers.md`
  When a repo already runs hooks, stardust adds itself to the existing chain rather than seizing `core.hooksPath`.
- **Sentinel-block idempotent hook injection** - `docs/adr/0008-sentinel-block-hook-injection.md`
  In compose mode, stardust writes its hook lines inside a sentinel-delimited block so install is idempotent and uninstall is exact.
- **Wrap the cobra tree with fang.Execute** - `docs/adr/0009-fang-cobra-execute.md`
  The stardust CLI shell runs through `fang.Execute(ctx, root, opts...)` instead of `cobra.Command.Execute()`, gaining styled help, a styled e...
- **Fang styles only chrome, data writers stay raw** - `docs/adr/0010-fang-headless-output-boundary.md`
  Fang governs help, usage, and error rendering only. Every headless data path keeps writing raw bytes to `cmd.OutOrStdout()`, so piped `--out...
- **Stardust cosmic colorscheme for fang chrome** - `docs/adr/0011-stardust-cosmic-colorscheme.md`
  The fang `ColorScheme` is mapped from the existing Stardust cosmic palette - violet primary with silver-white text - not a monochrome scheme...
- **A leaf clierr.Hint carries actionable errors** - `docs/adr/0012-cli-hint-error-type.md`
  A single `internal/clierr.Hint{Message, Suggestion, cause}` type carries every actionable CLI error, so any layer can return a clean problem...
- **Fang error handler renders a Run: suggestion line** - `docs/adr/0013-fang-error-handler-renders-suggestions.md`
  A `fang.WithErrorHandler` extracts a `clierr.Hint` with `errors.As` and renders the message, then the suggestion on its own highlighted `Run...
- **Single declarative per-collection schema is the one source of truth** - `docs/adr/0014-single-declarative-collection-schema.md`
  The per-collection `config.toml` schema is the only place doc field rules are declared; the scaffolder, checker, and autofixer all derive fr...
- **Related and inline path refs are first-class edges** - `docs/adr/0015-related-and-inline-refs-as-edges.md`
  Edge extraction covers wikilinks, `related:` frontmatter refs, and inline repo-path references, classified into doc-to-doc graph edges and d...
- **Vectors on by default, loud degradation, incremental by content hash** - `docs/adr/0016-vectors-on-by-default-loud-degradation.md`
  Semantic retrieval is on whenever Ollama is reachable, embeddings rebuild per changed chunk by content hash, a cross-encoder reranks the top...
- **Git-derived created and updated dates** - `docs/adr/0017-git-derived-created-updated.md`
  A doc's `created` is the date of its first commit and `updated` is the date of its last commit, computed from git rather than hand-typed.
- **Drift detection binds docs to referenced code by commit-distance** - `docs/adr/0018-drift-detection-by-commit-distance.md`
  A doc that references code through `governs:`, `related:`, or an inline path is flagged stale when that code has commits since the doc was l...
- **CI adopts via a baseline and ratchets on new errors only** - `docs/adr/0019-ci-baseline-ratchet.md`
  `stardust check --ci` reports and fails only on issues absent from a committed baseline, so a dirty repo adopts green and the gate catches n...
- **Plugin authoring commands reference the canonical forge skills** - `docs/adr/0020-authoring-commands-reference-canonical-forge-skills.md`
  The new `/stardust:spec`, `/stardust:plan`, `/stardust:doc`, and `/stardust:adr` commands name and route into the canonical the spec workflo...
- **Authoring commands delegate to skills and never reimplement the doc convention** - `docs/adr/0021-authoring-commands-delegate-never-reimplement.md`
  Each authoring command resolves workspace state and surfaces context, then hands off to the named canonical skill. It holds no embedded copy...
- **docs/plans is canonical and native /plan mirrors it** - `docs/adr/0022-docs-plans-canonical-native-plan-mirrors.md`
  The plan file at `docs/plans/<ts>-<slug>.md` is the single source of truth. Claude native `/plan` is a synced in-session mirror of it, and `...
- **Wikilinks resolve through collection-scoped names** - `docs/adr/0023-collection-scoped-link-namespacing.md`
  A wikilink may be qualified by its collection (the path-style `specs/slug` or the prefix-style `spec:slug`, written in double brackets) so t...
- **Plugin authoring commands bake forge workflows inline** - `docs/adr/0024-inline-baked-authoring-commands.md`
  The plugin authoring commands now embed the complete forge workflows inline and no longer hand off to `/the spec workflow` or `/the doc work...
- **Index-derived outputs reconcile against disk** - `docs/adr/0025-index-derived-outputs-reconcile-disk.md`
  Derived outputs MUST reconcile the SQLite index against markdown files on disk before presenting authoritative state. <details> <summary><b>...
- **Docs root stray markdown files are convention errors** - `docs/adr/0026-docs-root-stray-docs-are-errors.md`
  Markdown under `docs/` MUST live in exactly one registered collection folder unless it is `docs/INDEX.md` or a template. <details> <summary>...
- **Init auto-detects repo vs vault to default the docs convention** - `docs/adr/0027-init-auto-detect-default-policy.md`
  `stardust init` scaffolds the docs collections (specs, plans, adr, research) only when `--docs` is passed, with no detection and no way to a...
- **Status command gathers state in the service layer with an ANSI-free JSON contract** - `docs/adr/0028-status-command-and-json-contract.md`
  There is no `stardust status`. Checking whether a directory is initialized, what kind it is, which collections exist with what counts, wheth...
- **Copy and adapt the exo-jobs TUI framework** - `docs/adr/0029-copy-adapt-exo-jobs-tui-framework.md`
  Superseded by ADR 0033. The binding decision is now a verbatim copy of the exo-jobs TUI framework with only package/import path, palette, an...
- **Five-tab TabModel rebranded to Stardust functions** - `docs/adr/0030-five-tab-stardust-tui-model.md`
  The exo-jobs framework manages 14 jobs-domain tabs through a `TabModel` interface and an `int` active-tab index, with number keys jumping to...
- **TUI is additive to the no-arg TTY path only** - `docs/adr/0031-additive-default-path-tui.md`
  Stardust serves humans and agents from one binary. The hard constraint on the TUI rebuild is that the CLI subcommands, the MCP server (`star...
- **The TUI reads through the service layer** - `docs/adr/0032-tui-reads-through-service-layer.md`
  The current TUI's `backend` opens `index.Store` and `embed.Client` directly and the tabs call store methods (`Hybrid`, `Count`, `GetMeta`) a...
- **Verbatim copy of the exo-jobs TUI framework** - `docs/adr/0033-verbatim-copy-exo-jobs-tui-framework.md`
  ADR 0029 allowed the exo-jobs TUI framework to be copied and adapted. That left too much room for simplification: the first Stardust TUI att...
- **Settings is the sixth Stardust TUI tab** - `docs/adr/0034-sixth-settings-tui-tab.md`
  ADR 0030 fixed the interactive TUI at five tabs: Search, Browse, Graph, Drift, Status. None of them edits the per-vault `.stardust` config o...
- **Settings mutates config and runs index actions through additive service methods** - `docs/adr/0035-config-and-index-actions-through-service.md`
  ADR 0032 requires every TUI tab to read through `internal/service`, never the index, git, or disk directly. The Settings tab also writes: it...
- **Drift tab renders typed slices as single-header clean lists** - `docs/adr/0036-drift-renders-typed-slices-single-headers.md`
  `internal/tui/drift_tab.go` `markdownBox` prepends its own `# Drifted Docs` and `# Stale Docs` keycap headers, then appends `service.DriftRe...
- **Directory indexes are an opt-in managed-block convention** - `docs/adr/0037-directory-index-convention.md`
  Some Stardust vaults need local navigation files inside business folders. The global docs registry is still useful, but it does not replace...
- **Per-project vault resolution** - `docs/adr/0037-per-project-vault-resolution.md`
  The stardust plugin resolves the vault per project directory, not from one global path.
- **Librarian pass** - `docs/examples/cron-jobs/librarian/prompt.md`
  You are the vault librarian. Using the stardust CLI (`stardust query`, `stardust graph`, `stardust digest`), do a maintenance pass and surfa...
- **JSON-RPC typed contract rebuild - implementation plan** - `docs/plans/2026-06-25-0144-jsonrpc-typed-contract.md`
  Build the typed JSON-RPC registry in stardust, migrate exo-jobs onto the typed client, retire REST, and set up `.stardust` docs in both repo...
- **Hooks compose-not-clobber - implementation plan** - `docs/plans/2026-06-25-0345-hooks-compose-not-clobber.md`
  Make `hooks.Install` detect an existing hooks chain and append a sentinel block to it instead of seizing `core.hooksPath`, keeping owned-mod...
- **Fang CLI cosmic rebuild plus informative errors - implementation plan** - `docs/plans/2026-06-25-2319-fang-cli-cosmic.md`
  Wrap the stardust cobra tree with `fang.Execute`, theme its chrome with the cosmic colorscheme, add a `clierr.Hint` error type and a fang er...
- **Doc-code coherence engine** - `docs/plans/2026-06-26-0418-doc-code-coherence-engine.md`
  Transform Stardust from an indexer plus frontmatter linter into a doc-code coherence engine by closing three structural defects (Tier 1), ma...
- **Plugin authoring commands - implementation plan** - `docs/plans/2026-06-26-0418-plugin-authoring-commands.md`
  Add four thin router commands (`spec`, `plan`, `doc`, `adr`) under `plugin/claude/commands/` that resolve workspace state, surface relevant...
- **Stardust hardening implementation plan** - `docs/plans/2026-06-26-1849-stardust-hardening.md`
  Implement five hardening fixes with red-green TDD, then pass the full Go and plugin gates.
- **Init auto-detect and a status command** - `docs/plans/2026-06-26-2104-init-detect-and-status.md`
  Implement `stardust init` auto-detection of repo vs vault and a new `stardust status` command, TDD, one bite-sized task at a time.
- **Interactive Stardust TUI - implementation plan** - `docs/plans/2026-06-26-2352-interactive-tui.md`
  Replace `internal/tui` with a five-tab cosmic Bubble Tea shell copied and adapted from the exo-jobs framework, reading through `internal/ser...
- **Settings tab and Drift tab redesign** - `docs/plans/2026-06-27-0230-settings-tab-and-drift-redesign.md`
  Add a sixth Settings tab (config view/edit, collections + schema/counts, index actions) through additive service methods, and redesign the D...
- **Stardust v0.5.0 public release** - `docs/plans/2026-06-27-1813-stardust-v0-5-0-public-release.md`
  Goal: ship stardust public, brew-installable, v0.5.0, with full wiki mode and a zero-warning self-indexing docs vault. Architecture and cons...
- **Directory index convention implementation plan** - `docs/plans/2026-06-29-0229-directory-index-convention.md`
  Goal: implement the opt-in directory index convention, including config, service sync/check, CLI command, check integration, registry integr...
- **GitHub Wiki Compatibility** - `docs/research/2026-06-27-1721-github-wiki-compatibility.md`
  Thesis: Stardust is a strong fit for GitHub wiki search and health checks, but it needed wiki slug resolution, plain-frontmatter tolerance,...
- **JSON-RPC typed contract for stardust and exo-jobs** - `docs/specs/2026-06-25-0144-jsonrpc-typed-contract.md`
  Replace the stringly-typed REST + SDK seam between exo-jobs and stardust with one typed JSON-RPC 2.0 method registry served over both stdio...
- **Stardust hooks compose, never clobber** - `docs/specs/2026-06-25-0345-hooks-compose-not-clobber.md`
  When a repo already has a hooks manager or its own hooks, stardust appends its index and registry calls into the existing chain instead of s...
- **Fang-styled stardust CLI with the cosmic colorscheme** - `docs/specs/2026-06-25-2319-fang-cli-cosmic.md`
  Wrap the existing cobra tree with `fang.Execute` so help, usage, and errors render in the Stardust cosmic palette (violet accent, silver-whi...
- **Informative CLI errors across stardust** - `docs/specs/2026-06-25-2319-informative-cli-errors.md`
  Every stardust error that has a concrete next action carries a structured `Hint`, the fang error handler renders the suggested command on it...
- **Doc-code coherence engine** - `docs/specs/2026-06-26-0418-doc-code-coherence-engine.md`
  Stardust is an indexer and frontmatter linter today; it must become a doc-code coherence engine, because the hard problem in repo doc manage...
- **Plugin authoring commands for the the spec workflow and the doc workflow write path** - `docs/specs/2026-06-26-0418-plugin-authoring-commands.md`
  Four new slash commands (`/stardust:spec`, `/stardust:plan`, `/stardust:doc`, `/stardust:adr`) close the authoring loop by routing the user...
- **Stardust hardening for docs, index, links, and authoring commands** - `docs/specs/2026-06-26-1849-stardust-hardening.md`
  Stardust MUST stop producing confident derived state from stale inputs and MUST make its plugin authoring commands run the full docs workflo...
- **Init auto-detect and a status command** - `docs/specs/2026-06-26-2104-init-detect-and-status.md`
  `stardust init` MUST pick a sensible docs default by sniffing the target directory when neither `--docs` nor `--no-docs` is given, and a new...
- **Interactive Stardust TUI (exo-jobs framework, cosmic recolor)** - `docs/specs/2026-06-26-2352-interactive-tui.md`
  Replace the three-tab Stardust TUI with a five-tab Bubble Tea shell copied and adapted from the exo-jobs TUI framework, recolored to the cos...
- **Settings tab and Drift tab redesign** - `docs/specs/2026-06-27-0230-settings-tab-and-drift-redesign.md`
  Add a sixth Settings tab that views and edits the per-vault `.stardust` config, lists collections with schema and record counts, and runs in...
- **Stardust v0.5.0 public release** - `docs/specs/2026-06-27-1813-stardust-v0-5-0-public-release.md`
  Thesis: Ship stardust as a public, brew-installable v0.5.0 with full GitHub wiki mode and a self-indexing, zero-warning docs vault, executed...
- **Managed per-directory indexes** - `docs/specs/2026-06-29-0229-directory-index-convention.md`
  Stardust should let a vault opt into local `INDEX.md` files for selected directory trees. Those files are human-readable navigation aids, ge...
- **Vault resolution isolation per project** - `docs/specs/2026-06-30-0417-vault-resolution-isolation.md`
  Stardust vault mode keys the vault by project directory, so concurrent Claude sessions in different roots never collide on one global path....
- **Stardust audit command** - `docs/specs/2026-06-30-0418-stardust-audit-command.md`
  Thesis: `/stardust:audit` harvests the deterministic Stardust checks for the docs dimensions, fans out one subagent per code and policy dime...
- **Stardust - Claude Code plugin** - `plugin/claude/README.md`
  Makes stardust the context spine of a Claude Code session. It wires the stardust MCP server, injects a stardust-first policy plus live works...
- **Convention doc workflow** - `plugin/claude/commands/adr.md`
  You are `/stardust:adr`, the ADR shorthand for the convention doc workflow. Treat the doc type as `adr` and author the ADR inline in this tu...
- **Audit workflow** - `plugin/claude/commands/audit.md`
  You are `/stardust:audit`, the workspace audit command. It harvests the deterministic Stardust checks, fans out subagents over the dimension...
- **crons** - `plugin/claude/commands/crons.md`
  You are arming two native Claude Code crons for the stardust-plugin. They run only because the user invoked this command. Never create crons...
- **Convention doc workflow** - `plugin/claude/commands/doc.md`
  You are `/stardust:doc`, running the complete convention doc workflow inline for one document. Do not invoke the unrelated Microsoft `.docx`...
- **Spec and plan workflow** - `plugin/claude/commands/execute.md`
  You are `/stardust:execute`, the complete autonomous spec-and-build command. It runs the entire spec and plan workflow (write the spec, its...
- **The executable plan** - `plugin/claude/commands/plan.md`
  You are `/stardust:plan`, the executable-plan command. With no topic, list active plans from the resolved workspace. With a topic, author th...
- **refresh** - `plugin/claude/commands/refresh.md`
  You are refreshing the stardust index and registry for the resolved workspace. This is the manual path that the maintenance cron and the com...
- **setup** - `plugin/claude/commands/setup.md`
  You are configuring the stardust-plugin. The goal is a valid `config.json` under the plugin data directory and an initialized workspace, so...
- **Spec and plan workflow** - `plugin/claude/commands/spec.md`
  You are `/stardust:spec`, the spec authoring command. Author the spec and its ADRs in this turn, then regenerate the registry. This command...
- **status** - `plugin/claude/commands/status.md`
  You are reporting the stardust-plugin status for this session. Read-only. Keep it to a few lines.
- **Stardust - Obsidian plugin** - `plugin/obsidian/README.md`
  Surfaces Stardust's hybrid search inside Obsidian. A thin client over the local Stardust HTTP API (`stardust serve`) - all logic lives in th...
